spring:
  application:
    name: collaboration-service
  main:
    sources: com.by.open.ontology.collaborationservice.CollaborationServiceApplication
  data:
    mongodb:
      uri: mongodb://localhost:27017/webprotege
      database: webprotege

server:
  port: 8087

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin123@localhost:8762/eureka/
  instance:
    preferIpAddress: true