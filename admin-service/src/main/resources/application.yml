server:
  port: 8091

spring:
  application:
    name: admin-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/webprotege

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin123@localhost:8762/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    com.by.open.ontology: DEBUG