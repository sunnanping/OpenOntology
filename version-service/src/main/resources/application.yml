spring:
  application:
    name: version-service
  main:
    sources: com.by.open.ontology.versionservice.VersionServiceApplication
  data:
    mongodb:
      uri: mongodb://localhost:27017/webprotege
      database: webprotege

server:
  port: 8088

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin123@localhost:8762/eureka/
  instance:
    preferIpAddress: true