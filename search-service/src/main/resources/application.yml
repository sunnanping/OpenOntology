spring:
  application:
    name: search-service
  main:
    sources: com.by.open.ontology.searchservice.SearchServiceApplication
  data:
    mongodb:
      uri: mongodb://localhost:27017/webprotege
      database: webprotege

server:
  port: 8089

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin123@localhost:8762/eureka/
  instance:
    preferIpAddress: true